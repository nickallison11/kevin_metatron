{
  "name": "metatron-connect-base44",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:backend": "cargo run --package backend --bin backend",
    "dev:frontend": "npm run dev --workspace frontend"
  }
}
